// use winapi::um::winuser::{MessageBoxA, MB_ICONINFORMATION, MB_OK};

// let lp_text = std::ffi::CString::new(format!("{}", full_msg)).unwrap();
// let lp_caption = std::ffi::CString::new(format!("{}", text)).unwrap();

// unsafe {
//   MessageBoxA(
//     std::ptr::null_mut(),
//     lp_text.as_ptr(),
//     lp_caption.as_ptr(),
//     MB_OK | MB_ICONINFORMATION,
//   );
// }
